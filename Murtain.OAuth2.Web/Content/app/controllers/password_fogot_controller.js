'use strict';

define(['app', 'logger', 'browser'], function () {

    return ['$scope','logger','browser', function ($scope,logger,browser) {
        console.log('PasswordFogotController loading ...');
    }];

});